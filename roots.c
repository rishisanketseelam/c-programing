#include <stdio.h>
void main() {
	float x1, x2;
	int a, b, c;

	D= ((float)((b*2)-((4*a)*c)));
	x1=((float)-b)+((float)(D/(2*a)));
	x1=((float)-b)-((float)(D/(2*a)));
	printf("Please enter the value of A: ");
	scanf("%d",&a);
	printf("\nPlease enter the value of B: ");
	scanf("%d",&b);
	printf("\nPlease enter the value for C: ");
	scanf("%d",&c);
 	if (
}
