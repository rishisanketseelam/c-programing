#include <stdio.h>
void main() {
	float L, C, R, A, B, C, FRE;
	printf("Enter the resistance:");
	scanf("%f",%R);
	printf("Enter the capacitance:");
        scanf("%f",%C);
        printf("Enter the inductance:");
        scanf("%f",%L);
	A= (1/(L*C));
	B= (R/(2C)*2
	c=


}
