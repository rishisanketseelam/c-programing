#include <stdio.h>
void main (){
	int i;
	char ch;
        while(ch,i<=127) {
		if (ch<=127) {
                      printf(" ASCII character %d = \'%c\'\n",ch ,ch);
		      printf(" ASCII character %d = \'%c\'\n",ch ,ch);
	              ch= ch+1;
	              i = i+1;
		}
                else{ 
	          break;
		}  
             }	
}

